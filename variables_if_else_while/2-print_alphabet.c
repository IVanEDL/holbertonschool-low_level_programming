#include <stdio.h>

/**
 * main - this code will display all letters of the alphabet, lowercase
 *
 * Return: 0
 *
 */

int main(void)
{
	char c;

	for (c = 'a' ; c <= 'z' ; c++)
	{
		putchar(c);
	}
	return (0);
}
